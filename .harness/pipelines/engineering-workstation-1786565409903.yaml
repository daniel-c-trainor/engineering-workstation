pipeline:
  identifier: Build_daniel_c_trainor_engineering_workstation_1786565415766
  name: Build engineering-workstation
  orgIdentifier: default
  projectIdentifier: Engineering_Workstation_CI
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github_OAuth_1786565397777
        repoName: daniel-c-trainor/engineering-workstation
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: runlinter
                  name: run linter
                  spec:
                    command: |-
                      pip install flake8
                      flake8 .
                  timeout: ""
                  type: Run
              - step:
                  identifier: setupvirtualenvironment
                  name: setup virtual environment
                  spec:
                    command: |-
                      python3 -m venv .venv
                      . .venv/bin/activate
                      python3 -m pip install -r requirements.txt
                      python3 -m pip install -e .
                  timeout: ""
                  type: Run
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
